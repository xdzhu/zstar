# MoS2 electrostatic potential

Run `bash run.sh --dry-run` to inspect the command, then provide a converged
ABACUS cube with `bash run.sh --cube PATH`. The public `run/` directory
contains the matching ABACUS inputs and assets; compact reference plots and
profiles are in `results/`.
