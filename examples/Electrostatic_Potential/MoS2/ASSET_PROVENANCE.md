# Asset provenance

The ABACUS `INPUT`, `STRU`, `KPT`, pseudopotential, and orbital files in
`run/` are the compact public inputs used to define this example. The raw
electrostatic-potential cube is not redistributed; provide it with
`--cube PATH` after running the SCF calculation.
