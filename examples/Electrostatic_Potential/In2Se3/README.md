# alpha-In2Se3 electrostatic potential

Run `bash run.sh --dry-run` to inspect the command, then provide a converged
ABACUS cube with `bash run.sh --cube PATH`. The `run/` directory contains
the matching public ABACUS inputs and assets. The analysis adds the polar
arrow convention used for the out-of-plane slab response.
