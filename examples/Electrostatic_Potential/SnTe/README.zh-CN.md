# SnTe 静电势

本后处理案例展示面内 `a+b`、`a-b` 方向轮廓、z 方向轮廓、平面平均、5×5 扩胞
绘图和镜像检验。先运行 `bash run.sh --dry-run`，再使用
`bash run.sh --cube PATH` 传入兼容的收敛 cube。已有紧凑结果位于
`results/base/`，插值诊断位于 `results/interpolation/`。原始 cube 和上游 SCF
输入没有放入公开案例；详见 `run/README.md` 与 `ASSET_PROVENANCE.md`。
