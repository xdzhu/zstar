# CP2K H2O 光谱参考结果

这里保存 CP2K H2O 后端验证产生的精简 IR 和 Raman 结果。计算输入和运行说明
见 `examples/backend_examples/calculator_spectroscopy/cp2k_h2o`，
`spectra_results.json` 指向保留的模式表、谱线以及 PDF/SVG/PNG 图。

这些是参考输出，不是完整的 CP2K scratch 目录。需要重新生成时，请参阅
`examples/backend_examples/cp2k_bec/H2O/README.md` 和
`docs/calculator_spectroscopy.md` 中的 CP2K 命令。
