# Archived results

These compact outputs were collected from the completed VASP 6.3.2/PBEsol
workflow. `spectra_results.json` retains the BEC, electronic dielectric, mode,
and Raman tensors; the channel directories contain tables, broadened data, and
PNG/PDF/SVG plots. Licensed `POTCAR` and large VASP scratch files are excluded.
