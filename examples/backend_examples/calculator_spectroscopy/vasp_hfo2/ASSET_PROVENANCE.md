# Asset provenance

VASP `POTCAR` is distributed under the VASP license and is intentionally not
included. Reproduction requires a locally licensed Hf_pv/O PAW PBE `POTCAR`
whose species order matches `run/POSCAR`. The public case retains only the
author-created inputs, compact parsed tensors, spectra, and plots.
