# Asset provenance

The CP2K GTH pseudopotentials and basis sets are resolved from the installed
CP2K data directory named by `CP2K_DATA_DIR`. They are intentionally not
duplicated in the repository; the exact PBE input and retained outputs are
under `run/` and `results/`.
