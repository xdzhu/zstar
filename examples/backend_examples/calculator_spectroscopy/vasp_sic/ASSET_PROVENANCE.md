# Asset provenance

VASP `POTCAR` and the completed phonon `vasprun.xml` must be supplied by the
user under the VASP license. Place them in `run/` together with `INCAR`,
`POSCAR`, and `KPOINTS`; they are intentionally absent from the public case.
The retained response tables and plots in `results/` are the distributable
reference outputs.
